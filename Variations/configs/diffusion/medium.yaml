experiment_name: medium
run_name: medium
seed: 42
extraction_root: Variations/outputs/extraction/medium
output_root: Variations/outputs/diffusion
splits:
  val_fraction: 0.1
  seed: 42
  min_pairs_per_split: 1000
data:
  batch_size: 512
  num_workers: 4
model:
  hand_dim: 46
  hidden_dim: 512
  num_blocks: 8
  time_dim: 256
  cond_dim: 512
diffusion:
  timesteps: 1000
  beta_start: 0.0001
  beta_end: 0.02
  ddim_steps: 50
training:
  epochs: 200
  learning_rate: 0.0001
  weight_decay: 0.0001
  warmup_epochs: 5
  amp: true
  ema_decay: 0.999
  val_every: 1
  checkpoint_interval: 5
  best_of_k: 8
  max_val_batches: 16
  sample_artifact_count: 32
  device: auto
wandb:
  enabled: true
  project: variations
  job_type: diffusion
  tags:
    - variations
    - diffusion
    - joints-only
  log_artifacts: true
  watch_model: true
  watch_log: gradients
  watch_log_freq: 100
