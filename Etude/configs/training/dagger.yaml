training:
  mode: dagger

rollout_training:
  enabled: false
  replay_buffer_capacity: 50000
  failure_only_sampling: true
  failure_window_size: 16

dagger:
  enabled: false
  export_in_memory_only: true
  max_failure_windows: 128

reward:
  target_key_activation: 2.0
  missed_key: -3.0
  wrong_key: -2.0
  fingertip_tracking: -0.25
  joint_tracking: -0.1
