# Config Schema Placeholders

This directory reserves schema-oriented config fragments for future Etude agents.

- Keep schema files lightweight and declarative.
- Expose feature modules by import path, for example `etude.features.key_blocks:build_key_features`.
- Do not add simulator-dependent logic to schema files.
